twig:
    default_path: '%kernel.project_dir%/templates'
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'

    globals:
        algolia_place_app_id: "%algolia_place_app_id%"
        algolia_place_api_id: "%algolia_place_api_id%"
