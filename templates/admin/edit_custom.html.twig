{% extends '@EasyAdmin/default/edit.html.twig' %}

{% block head_custom_stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('admin') }}
{% endblock %}

{% block body_custom_javascript %}
    {{ parent() }}

    <script type="text/javascript">
        var lat = document.querySelector("[id$='location_lat']");    
        var lng = document.querySelector("[id$='location_lng']");
        var locale = "{{ app.request.locale }}";
		var algolia_place_app_id = "{{ algolia_place_app_id }}";
		var algolia_place_api_id = "{{ algolia_place_api_id }}";
    </script>

    {{ encore_entry_script_tags('admin') }}

{% endblock %}