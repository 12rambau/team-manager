{% for social in socials %}
	<a 
        href="{{ social.url }}" 
        onmouseover="this.style.background='{{ social.color }}';"
        onmouseout="this.style.background='rgba(255, 255, 255, 0.3)';">
		<i class="fab fa-{{ social.icon }}"></i>
	</a>
{% endfor %}
